{
  "name": "indonesia-regions",
  "version": "1.0.0",
  "description": "Express.js service for Indonesian administrative region data",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "generate:regions": "node scripts/generate-regions.js",
    "validate:regions": "node scripts/validate-regions.js",
    "cloudflare:dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "keywords": [
    "express",
    "node"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
